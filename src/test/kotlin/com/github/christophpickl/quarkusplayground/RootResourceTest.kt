package com.github.christophpickl.quarkusplayground

// @QuarkusTest
class RootResourceTest {
    // @Test
    fun `asf asdf`() {
        /*
        given()
          .`when`().get("/hello")
          .then()
             .statusCode(200)
             .body("message", equalTo("hello"))
         */
    }
}